#question by Sigma
###
from graphics import *
def main():
        win=GraphWin('vector',600,600)
        win.setCoords(-0.1,-0.1,1.1,1.1)
        for p in range(1000):
            p+=1
            
            for q in range(p,100):
                q+=1
                if q%p!=0 :
                    Point(1.0*p/q,1.0/p).draw(win)
                    #print 1.0*p/q,1.0/p



main()
